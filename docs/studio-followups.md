# PYTTI STUDIO — review followups (medium/low, deliberately deferred)

From the 14-agent adversarial review. Critical/high findings were all fixed
(server: pump guard+draft coercion, EventHub identity, resume lock, process
groups, tqdm gating, atomic queue/preempt; frontend: see app.js fix commit).
These remain, ordered as reported:

- [ ] **[medium] server.py**: Frames/thumbs served mid-write and cached immutable: torn PNGs live forever in the browser cache
  - _watch_frames publishes a frame event the instant the file appears in iterdir(), but ImageGuide._save_frame does a direct `im.save(path)` — no tmp+rename — so at 1080p+ a PNG can still be mid-write when the browser fetches it. _send_file then serves the truncated file with `Cache-Control: public, max-age=31536000, immutable` (line 1131), so the broken image is cached permanently under that URL — i
  - fix: In _watch_frames, only publish a frame once its size is stable across two polls (or mtime older than ~1s); better, patch _save_frame in pytti-core to save to a .tmp and rename (you own it now). Generate thumbs to a tmp path and os.replace; wrap Image.open failures as a 404-retry rather than 500. Dro
- [ ] **[medium] server.py**: DELETE session while an encode runs: rmtree under ffmpeg, and STORE.update KeyError kills the encode's terminal SSE event
  - do_DELETE only refuses deletion for MANAGER.live_id; running encode jobs aren't checked. STORE.delete rmtree's the run dir while ffmpeg reads frames from it and writes the artifact into it. Then in EncodeManager._pump: on the 'done' path line 987 `STORE.update(sid, ...)` raises KeyError (session popped from the store), the pump thread dies before line 988, and the terminal 'encode' event is never 
  - fix: In do_DELETE, 409 when ENCODER has a running job for that sid (track jobs per session); in _pump wrap the store update in try/except KeyError and always publish the terminal event in a finally; pop finished jobs from self.jobs after publishing.
- [ ] **[medium] server.py**: stop() racing _finalize can overwrite the terminal state with 'stopping' — session wedged in a non-terminal state
  - stop() validates live_id under the lock but performs STORE.update(state='stopping') after releasing it. If the process exits at that moment, the pump thread's _finalize can interleave: finalize writes state='done'/'failed' + endedAt, then stop's update stamps state='stopping' over it (STORE.update patches state only, leaving endedAt/exitCode from the finalize). No process exists any more, so nothi
  - fix: Do the STORE.update/publish inside the same lock as the live_id check, and make _finalize's terminal write win: have STORE.update refuse to overwrite a terminal state (done/stopped/failed) with a non-terminal one, or re-check `self.live_id == session_id` under the manager lock immediately before wri
- [ ] **[medium] server.py**: Orphan recovery: alive-pid sessions are left unmanageable, and PID reuse fakes 'alive'
  - When orphan_recovery finds a pid that answers os.kill(pid, 0) it 'leaves it alone': the session stays in state 'rendering' but MANAGER never adopts it — stop() raises KeyError (404 in UI), no pump thread reads its output, no _watch_frames publishes its frames, no finalize will ever run, and because MANAGER.proc is None a NEW render can be started concurrently on the same GPU. Separately, pid files
  - fix: Strengthen liveness: verify the pid's identity (psutil create_time vs sidecar startedAt, or store proc start time in the pid file, or check the process cmdline contains 'pytti.workhorse' and the sid). For genuinely-alive orphans either re-adopt them (spawn a watcher on the pid + frames dir, allow ki
- [ ] **[low] server.py**: Resume step accounting over-counts by skip_steps for every scene after a mid-scene resume
  - Workhorse restores at i = restore_frame*save_every, then the FIRST resumed scene's tqdm bar runs only steps_per_scene - (i % steps_per_scene) steps (workhorse.py:436-448: skip_steps = i % steps_per_scene). The server maps bar position with `step = resume_offset + scene*steps_per_scene + bar_step` (line 720), which is correct for the first resumed scene but adds a full steps_per_scene for each subs
  - fix: Track per-bar totals: the regex already captures _bar_total (line 717, unused). Maintain a cumulative offset: on each 'Running prompt:' add the PREVIOUS bar's total (or its last seen bar_step) to a base counter instead of multiplying scene index by steps_per_scene; step = resume_offset_base + bar_st
- [ ] **[low] server.py**: HTTP/1.1 keep-alive desync: early-return paths never read the request body
  - protocol_version is HTTP/1.1 (keep-alive default) but several handlers respond without consuming the request body: do_POST's unmatched-path 404 (line 1327), do_PUT's 404, and _post_upload's non-multipart 400 (line 1369). The unread body bytes stay in rfile and are parsed as the next request's request-line on the same connection, so the browser's next legitimate API call on that socket gets a garba
  - fix: Read (and discard) Content-Length bytes before writing any response — simplest is to call self._read_body() at the top of do_POST/do_PUT — or set self.close_connection = True on every error path that may not have drained rfile; in the do_GET catch-all, only attempt the 500 if headers haven't been se
- [ ] **[low] server.py**: Unlocked writers share one tmp path: concurrent draft PUTs / preset saves / studio POSTs can tear files; slow SSE clients grow unbounded queues
  - Grab-bag of smaller leaks and races. (a) atomic_write uses a fixed tmp name (path + '.tmp'); STORE and CALIBRATION serialize their writes with locks, but write_draft (1023), preset save (1315), and /api/system's read-modify-write (1321-1325) do not — two concurrent PUT /api/draft (autosave debounce racing a tab close) interleave write_text/rename on the same tmp inode, risking a truncated draft.ya
  - fix: (a) Use tempfile.NamedTemporaryFile(dir=path.parent, delete=False) + os.replace in atomic_write so tmp names are unique, and add a small module lock around draft/studio read-modify-write. (b) Cap client deques (maxlen ~2000) and drop-oldest, or disconnect clients whose queue exceeds the cap. (c) Pop
- [ ] **[medium] style.css**: CSS class rules and per-frame inline styles fight over the same attribute — two spec'd stylings are dead
  - Two concrete instances of the ui.md violation 'if an attribute's dynamic, own it fully in JS; avoid cascading attribute overrides': (1) style.css:183 `.lib-card.selected { box-shadow: var(--glow-focus) }` can never apply because projectCard writes rec.node.style.boxShadow inline every frame (app.js:2660, `isLive ? pulse-glow : 'none'`) — inline always wins, so selected cards never get their glow. 
  - fix: Fold both into the single inline write and delete the CSS declarations: `rec.node.style.boxShadow = isLive ? pulseGlow : selected ? 'var(--glow-focus)' : 'none'`; `row.row.style.opacity = ER_READONLY.has(name) ? '0.6' : isRelevant(name) ? '1' : '0.45'`.
- [ ] **[medium] app.js**: state.dirty is written everywhere and read exactly once — every chunk re-projects the whole app on every pointermove
  - Only renderSparklineChunk consults the dirty map (app.js:2948); all other chunks run unconditionally each frame. Concrete per-frame costs on any pointermove: tokenizeScenes over the entire scenes text (app.js:3054); renderLibrary calls dc.cards.appendChild(rec.node) for every visible card (app.js:2714) — the comment claims 'no-op when already last-appended' but appendChild is a MOVE for every non-
  - fix: Pick one: (a) make each render chunk early-return unless its dirty bits or an active animation require a repaint (the flags are already being set correctly in tick), or (b) delete state.dirty entirely and keep full projection — but then eliminate the churn: guard appendChild behind a cached order si
- [ ] **[medium] app.js**: fsFollow reads track.offsetWidth in the side-effects phase after writing its width — a forced reflow per saved frame, to rediscover a number the code just computed
  - renderFilmstrip writes the track width as frames * FS_THUMB_STEP (app.js:2861) and arms fx.fsFollow on every new live frame (app.js:2887); applyFx then reads domCache.filmstrip.track.offsetWidth (app.js:2551) after all DOM writes — a synchronous layout flush every time a frame is saved during a live render. Violates ui.md read/write batching and engineering.md derive-don't-rediscover: both the wid
  - fix: Have renderFilmstrip stash the computed width (or frame count) into fx, e.g. fx.fsFollow = frames * FS_THUMB_STEP, and applyFx assign scrollLeft from that value — no DOM read needed.
- [ ] **[medium] app.js**: Boundary validation is partial — several SSE/REST fields flow through as strings-as-any and get re-defaulted downstream
  - engineering.md: parse & validate fully at the edge; don't half-parse then ad-hoc-check downstream. Gaps: validateSse('progress') (app.js:334-338) skips scene/sceneCount/phase/nextFrameInSec — applySse re-defaults them with ?? (app.js:1777-1780), so a bad `phase` string silently renders as the SCENE branch (app.js:2937-2941 default) instead of failing loud. 'log' kind (app.js:344) is any string, in
  - fix: Extend each validator to cover every field the client reads: enumerate the unions (phase, log kind, encode state) exactly like the 'state' event does; use opt(...,'number') for optional numerics; validate artifact items, preflight estimate/issues, browse entries, presets, and the 409 body. Then dele
- [ ] **[low] app.js**: PiP, library-card, and archive thumbs swap src without decode() — partial paints on hot swap paths
  - media.md: wait for img.decode() before painting to avoid showing a partially decoded image. The stage does this correctly via the back buffer (app.js:2780-2787), but renderPip swaps src on every saved live frame (app.js:2836), and hover-scrub swaps library thumb src per sampled pointermove position (app.js:2667) and archive posters likewise (app.js:3404) — all directly on the displayed <img>.
  - fix: Reuse the stage's pattern for the PiP (tiny second img + decode().then(emit) is already wired); for hover-scrub thumbs, preload with `new Image()` + decode() and only then assign src, or accept the previous frame staying until decode resolves.
- [ ] **[low] app.js**: Clipboard write failure is silenced while the toast already claimed success
  - applyFx runs navigator.clipboard.writeText(fx.copyText).catch(() => {}) (app.js:2570), but the FINDER actions toast 'path copied: …' unconditionally before the write happens (app.js:1861, 1867). engineering.md forbids silencing recoverable failures — this one is silenced AND the UI asserts the opposite.
  - fix: Move the toast into the promise: .then(() => emit({k:'copied', path})) / .catch((e) => emit({k:'error', message:'clipboard write failed: ' + e.message})), and let tick raise the toast from the event.
- [ ] **[low] app.js**: Dead state and misplaced plumbing: confirm spring, hash(), firstBoot, sse.lastEventId, saveTimer inside state
  - engineering.md: unused/duplicated state falsely enlarges the state space and future maintainers can't tell if it's load-bearing. (1) anim.springs.confirm (app.js:156) is never given a dest — renderToastConfirm (app.js:3938) uses no spring; the confirm dialog pops with no animation. (2) hash() (app.js:26-30) has zero callers. (3) state.firstBoot (app.js:67) is written at app.js:4013 and never read 
  - fix: Delete hash(), springs.confirm (or actually animate the confirm with it), state.sse.lastEventId, and state.firstBoot (use a local in boot()); move saveTimer into the `timers` object next to timers.preflight.
- [ ] **[low] app.js**: Phosphor decay: DOM read mid-write-phase, anim state mutated after `still` was computed, and CSS + JS both animating the same color
  - renderProgressDeck detects change by reading dc.step.textContent during the write phase (app.js:2932) — a DOM read interleaved into writes and a rediscovery of a value the code is about to set (should be a cached key like projectCard.metaKey). decayMark then mutates anim.decays (app.js:166) AFTER animTick already computed `still` (app.js:2451-2456), so the fade's continued rendering silently depen
  - fix: Mark decays in tick when the progress event is applied (tick already knows step changed), compare against a domCache lastStepText key instead of reading textContent, and pick one animation owner — drop the CSS transition and let decayColor interpolate, or keep CSS and have JS write the target color 
- [ ] **[low] app.js**: Network fetches fired from inside a render chunk (inspector)
  - renderInspectorChunk calls loadDetail during the DOM-write phase: app.js:3489 (diff target) and app.js:3517 (own config). ui.md's loop reserves side effects for the dedicated phase after DOM writes, and render is meant to be a pure projection (rewindable/debuggable). The pendingDetails guard makes it not-a-fetch-storm, but it couples 'painting the inspector' to 'issuing HTTP requests' — and with t
  - fix: Request details from tick when the triggering state changes — the 'inspect'/'stage-inspect' actions and the inspector-diffsel ui handler already run in tick and mostly do this; add loadDetail there for the diff target and delete both calls from the render chunk.
- [ ] **[medium] style.css**: PiP is buried under the archive overlay — the live render is invisible while browsing the archive
  - Present-but-wrong (spec §3.3 ①: 'whenever the stage shows anything other than the live session, the live feed shrinks to a corner PiP… The render is never invisible'; §2.2 stage row: 'PiP of live render when browsing elsewhere'). #pip lives inside #stage (z-index 5 in the stage stacking context) while #archive is position:fixed z-index 20 covering everything left of the bench. Opening the archive 
  - fix: Move #pip to be a direct child of <body> (position:fixed, bottom-right, z-index above #archive's 20 but below sheets), and extend renderPip's visibility condition to include state.ui.archiveOpen && live.sessionId != null.
- [ ] **[low] app.js**: EXPOSING placard computes 'first frame at step N' from the draft, not the live session's config
  - Present-but-wrong (spec §3.2: placard is 'driven by state/progress events' about the live run; §9: 'the bench belongs to the next session'). The placard reads state.draft.values.steps_per_frame (app.js:2805) — the draft is explicitly the future session, which the user may have already edited or forked from something else while this render launches. Queued sessions run their snapshot, not the curre
  - fix: Read steps_per_frame (and save_every) from the live session's config: sessionOf(live.sessionId).detail?.config, falling back to fetching detail when live starts (loadDetail(live.sessionId) on the first launching event), not from draft.values.
- [ ] **[low] server.py**: Preflight issues on engine-room-only fields route to a bench section that doesn't contain the field
  - Present-but-wrong (spec §9: 'issues carry {field, section} so the bench badges the right section and clicking an issue scrolls+focuses the field'). 32 annotations entries have section: null (e.g. cutouts, gradient_accumulation_steps); preflight coerces null → 'engine' (server.py:280). issue-jump then opens the bench ENGINE section — which contains only CLIP preset chips — and fx.scrollToField find
  - fix: In doAction 'issue-jump' (and doToastEvent), when domCache.bench.widgets lacks the field, open the engine room with engineRoomSearch = field (the 'help-setting' action already does exactly this).
- [ ] **[low] app.js**: Rerolling the seed while unlocked displays a seed the server will discard
  - Present-but-wrong (spec §4.1 seed: 'actual seed always displayed (server bakes and returns it — never blank)'; §2.1 lock/die semantics). rerollSeed writes a new seed into the draft but does not set seedLocked. RenderManager.start (server.py:547) rerolls whenever seed_locked is false, so the number the user just rolled and is looking at in the seed strip is not the seed the session runs with — the 
  - fix: Make the die an explicit act per the spec: 'seed-roll' should set state.draft.seedLocked = true along with the new value (one-line change in doAction). Optionally render the seed dimmed with a '~' prefix whenever unlocked so an about-to-be-rerolled seed never reads as authoritative.
- [ ] **[low] app.js**: CLIP presets and checkboxes are not costed — spec requires measured s/step on each
  - Missing entirely (spec §4.1 ENGINE: 'Costed presets: DRAFT/BALANCED/RICH, each with measured s/step at current resolution'; §3.5: each raw CLIP checkbox '+11.0 s/st… (measured @ 512px)'; §4.2 SEEING: 'each with measured per-model s/step delta'). The bench ENGINE section renders bare DRAFT/BALANCED/RICH chips and a model list; the engine room shows only the static costNote strings from annotations.
  - fix: Annotate each preset chip and engine-room CLIP row with the calibration bucket lookup for the current width×height and resulting clip count (state.calibration.buckets[`${w}x${h}/${n}clip/${model}`], same pattern as calibBadge at app.js ~3002), falling back to '—' when unmeasured.
